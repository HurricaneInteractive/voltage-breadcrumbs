import React from "react"

import logo from "../logo.svg"

const Hero = () => (
  <div className="hero">
    <img src={logo} alt="Voltage logo mark" />
    <h1>Voltage <span>Breadcrumbs</span></h1>
  </div>
)

export default Hero
